---
phase: 03-player-statistics
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - frontend/src/components/BracketMatch.jsx
  - frontend/src/components/RankingsTable.jsx
autonomous: true
requirements:
  - STATS-02

must_haves:
  truths:
    - "Player names in BracketMatch are clickable links navigating to /players/:id"
    - "Player names in RankingsTable are clickable links navigating to /players/:id"
    - "BYE slots and TBD placeholders are not linked (no id available)"
    - "Pair entries in RankingsTable link each individual player name separately"
    - "Clicking a player name in the bracket or rankings opens their public profile page"
  artifacts:
    - path: "frontend/src/components/BracketMatch.jsx"
      provides: "Clickable player name links using react-router-dom Link"
      contains: "Link"
    - path: "frontend/src/components/RankingsTable.jsx"
      provides: "Clickable player name links in name column cell renderer"
      contains: "Link"
  key_links:
    - from: "frontend/src/components/BracketMatch.jsx"
      to: "/players/:id"
      via: "react-router-dom Link wrapping player-name span"
      pattern: "Link.*players"
    - from: "frontend/src/components/RankingsTable.jsx"
      to: "/players/:id"
      via: "react-router-dom Link in name cell renderer"
      pattern: "Link.*players"
---

<objective>
Make player names in BracketMatch and RankingsTable clickable links to their public profile pages at /players/:id — completing the navigation discovery path from bracket and rankings into player profiles.

Purpose: Locked decision from CONTEXT.md: "Player names in the knockout bracket and in the rankings table become clickable links to their profile page." This plan runs in Wave 2 parallel with Plan 02 (no file overlap).
Output: 2 existing components modified with react-router-dom Link wrapping player name spans.
</objective>

<execution_context>
@C:/Users/erich/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/erich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-player-statistics/03-CONTEXT.md
@.planning/phases/03-player-statistics/03-RESEARCH.md

<interfaces>
<!-- BracketMatch.jsx current player name rendering -->
From frontend/src/components/BracketMatch.jsx:
```jsx
// Top player (player1):
<span className="player-name">
  {getPlayerName(match.player1, match.pair1, isDoubles)}
  {match.player1?.seed && <span className="seed-badge">[{match.player1.seed}]</span>}
</span>

// Bottom player (player2):
<span className="player-name">
  {getPlayerName(match.player2, match.pair2, isDoubles)}
  {match.player2?.seed && <span className="seed-badge">[{match.player2.seed}]</span>}
</span>

// match.player1 shape: { id: string, name: string, seed?: number }
// match.player2 shape: { id: string, name: string, seed?: number }
// match.pair1 shape: { id, name, player1: { id, name }, player2: { id, name } }
// For doubles: getPlayerName returns pair.name or "p1.name / p2.name"
// Link only applies for singles (isDoubles=false), when player id is available
// BYE/TBD: match.player1 is null — no link in that case
```

<!-- RankingsTable.jsx current name cell renderer -->
From frontend/src/components/RankingsTable.jsx:
```javascript
{
  id: 'name',
  header: t('table.headers.name'),
  cell: info => {
    const entry = info.row.original;
    if (entry.entityType === 'PLAYER') {
      return entry.player?.name || t('common.unknown');
    } else if (entry.entityType === 'PAIR') {
      const p1 = entry.pair?.player1?.name || '?';
      const p2 = entry.pair?.player2?.name || '?';
      return `${p1} / ${p2}`;
    }
    return t('common.unknown');
  }
}
// entry.player shape: { id: string, name: string } (id available for link)
// entry.pair.player1 shape: { id: string, name: string }
// entry.pair.player2 shape: { id: string, name: string }
// Use react-router-dom Link to wrap names
```

<!-- react-router-dom Link import pattern -->
import { Link } from 'react-router-dom';
// Usage: <Link to={`/players/${playerId}`}>{playerName}</Link>
// Only render Link when id is available — fall back to plain text if not
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make player names clickable in BracketMatch.jsx</name>
  <files>frontend/src/components/BracketMatch.jsx</files>
  <action>
Add `import { Link } from 'react-router-dom';` at the top of BracketMatch.jsx alongside the existing imports.

Modify the top player `<span className="player-name">` block to wrap the name with a Link when the player has an id and it is NOT a doubles match:

```jsx
<span className="player-name">
  {!isDoubles && match.player1?.id ? (
    <Link to={`/players/${match.player1.id}`} onClick={e => e.stopPropagation()}>
      {getPlayerName(match.player1, match.pair1, isDoubles)}
    </Link>
  ) : (
    getPlayerName(match.player1, match.pair1, isDoubles)
  )}
  {match.player1?.seed && <span className="seed-badge">[{match.player1.seed}]</span>}
</span>
```

Apply the same pattern for the bottom player (player2):

```jsx
<span className="player-name">
  {!isDoubles && match.player2?.id ? (
    <Link to={`/players/${match.player2.id}`} onClick={e => e.stopPropagation()}>
      {getPlayerName(match.player2, match.pair2, isDoubles)}
    </Link>
  ) : (
    getPlayerName(match.player2, match.pair2, isDoubles)
  )}
  {match.player2?.seed && <span className="seed-badge">[{match.player2.seed}]</span>}
</span>
```

Key constraints:
- `e.stopPropagation()` on the Link click prevents the match click handler (for score entry modal) from firing when user clicks a name link.
- For doubles matches (`isDoubles=true`), `getPlayerName` returns the pair name as a plain string — do NOT link it (pair entity does not have a public profile page).
- For BYE/TBD slots where player is null, the ternary falls through to plain text "TBD".
- Do NOT change any other logic in BracketMatch.jsx — only add the Link import and modify the two `<span className="player-name">` blocks.
  </action>
  <verify>
    <automated>cd D:/Workspace/BATL && grep -n "from 'react-router-dom'" frontend/src/components/BracketMatch.jsx</automated>
  </verify>
  <done>BracketMatch.jsx imports Link from react-router-dom; single-player name spans wrap in Link when player.id is present and isDoubles=false</done>
</task>

<task type="auto">
  <name>Task 2: Make player names clickable in RankingsTable.jsx</name>
  <files>frontend/src/components/RankingsTable.jsx</files>
  <action>
Add `import { Link } from 'react-router-dom';` at the top of RankingsTable.jsx alongside the existing imports.

Replace the `name` column cell renderer with a version that wraps player names in Links:

```javascript
{
  id: 'name',
  header: t('table.headers.name'),
  cell: info => {
    const entry = info.row.original;
    if (entry.entityType === 'PLAYER') {
      const name = entry.player?.name || t('common.unknown');
      const id = entry.player?.id;
      return id ? <Link to={`/players/${id}`}>{name}</Link> : name;
    } else if (entry.entityType === 'PAIR') {
      const p1Name = entry.pair?.player1?.name || '?';
      const p1Id = entry.pair?.player1?.id;
      const p2Name = entry.pair?.player2?.name || '?';
      const p2Id = entry.pair?.player2?.id;
      return (
        <>
          {p1Id ? <Link to={`/players/${p1Id}`}>{p1Name}</Link> : p1Name}
          {' / '}
          {p2Id ? <Link to={`/players/${p2Id}`}>{p2Name}</Link> : p2Name}
        </>
      );
    }
    return t('common.unknown');
  }
}
```

For PAIR entries, each player's name links individually — e.g., "Alice" links to /players/alice-id and "Bob" links to /players/bob-id, separated by " / ".

Do NOT change any other logic in RankingsTable.jsx.
  </action>
  <verify>
    <automated>cd D:/Workspace/BATL && grep -n "from 'react-router-dom'" frontend/src/components/RankingsTable.jsx</automated>
  </verify>
  <done>RankingsTable.jsx imports Link from react-router-dom; PLAYER entry names link to /players/:id; PAIR entry renders two individual links separated by " / "</done>
</task>

</tasks>

<verification>
- BracketMatch.jsx imports Link from react-router-dom
- RankingsTable.jsx imports Link from react-router-dom
- Singles player names in bracket are wrapped in Link to /players/:id
- Doubles match names in bracket are plain text (no link — pair has no public profile)
- PLAYER ranking entries link to /players/:id
- PAIR ranking entries show two individual links: "p1Name / p2Name"
- BYE and TBD slots (null player) remain as plain text
- Link click in bracket stops propagation to prevent score modal from opening
</verification>

<success_criteria>
- Clicking a singles player name in the knockout bracket navigates to /players/:id
- Clicking a player name in the rankings table navigates to /players/:id
- Score entry modal does NOT open when clicking a player name link in the bracket
- Doubles pair names in bracket remain unlinked plain text
- No existing functionality in BracketMatch or RankingsTable is broken
</success_criteria>

<output>
After completion, create `.planning/phases/03-player-statistics/03-03-SUMMARY.md`
</output>
