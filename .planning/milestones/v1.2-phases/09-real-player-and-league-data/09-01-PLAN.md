---
phase: 09-real-player-and-league-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/data/players.js
  - backend/prisma/seed.js
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - ACCT-01
  - ACCT-02
  - ACCT-03
  - PAIR-01
  - PAIR-02
  - LOC-01
  - LOC-02
  - TOURN-01

must_haves:
  truths:
    - "34 real named players exist in seed output (18 male, 16 female)"
    - "Erich Siebenstich ml. has an ADMIN user account at erich@batl"
    - "Rene Parak has an ORGANIZER user account at rene@batl"
    - "Existing generic accounts (admin@batl.example.com, organizer@batl.example.com, player@batl.example.com) still work"
    - "18 mixed doubles pairs created in Mixed Doubles Open category"
    - "All 34 real players registered in Mixed Doubles Open category"
    - "Only one location exists: ProSet"
  artifacts:
    - path: "backend/prisma/data/players.js"
      provides: "Real player data arrays, pair definitions, email mappings"
      exports: ["malePlayers", "femalePlayers", "mixedPairs"]
    - path: "backend/prisma/seed.js"
      provides: "Database seed script using real player data"
      contains: "import.*from.*data/players"
  key_links:
    - from: "backend/prisma/seed.js"
      to: "backend/prisma/data/players.js"
      via: "ES module import"
      pattern: "import.*from.*[./]*data/players"
    - from: "backend/prisma/seed.js"
      to: "prisma.playerProfile.upsert"
      via: "iteration over imported player arrays"
      pattern: "malePlayers|femalePlayers"
    - from: "backend/prisma/seed.js"
      to: "prisma.doublesPair.create"
      via: "iteration over imported pair definitions"
      pattern: "mixedPairs"
---

<objective>
Create the real player data file and rewrite the foundation sections of seed.js to use real league data.

Purpose: Replace all placeholder/generic player data, locations, and pair definitions with actual BATL league membership data (34 real players, 18 mixed doubles pairs, ProSet location, linked accounts for Erich and Rene).

Output: A standalone data file at `backend/prisma/data/players.js` and a rewritten `backend/prisma/seed.js` that creates real players, accounts, pairs, category registrations, and the ProSet location.
</objective>

<execution_context>
@C:/Users/erich/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/erich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-real-player-and-league-data/09-CONTEXT.md
@notes/mix-doubles-data-2026.md
@backend/prisma/seed.js
@backend/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create real player data file</name>
  <files>backend/prisma/data/players.js</files>
  <action>
Create a new directory `backend/prisma/data/` and file `players.js` as an ES module exporting all league data.

The file must export these named arrays/objects:

**`malePlayers` (18 entries)** — Array of objects, each with:
- `name`: Full name exactly as in `notes/mix-doubles-data-2026.md`
- `email`: Following the `firstname@batl` convention from CONTEXT.md decisions
- `gender`: `'MEN'`
- `birthDate`: Approximate DOB as `new Date('YYYY-MM-DD')` in the 30-60 year range (born ~1966-1996). Cluster DOBs so that enough players qualify for 35+, 40+, and 50+ categories (at least 4-5 players per age group).

Name collision handling per CONTEXT.md decisions:
- Vlado Zvonar: `vlado@batl`, Vlado Fatura: `vlado.f@batl`, Vlado Koco: `vlado.k@batl`
- Roman Rummel: `roman@batl`, Roman Smahovsky: `roman.s@batl`
- Marcel Sramko: `marcel@batl`, Marcel Pesko: `marcel.p@batl`
- Miro Uhliar: `miro@batl`, Miro Petrech: `miro.p@batl`

Order: Tomas Zaprazny, Laco Stevko, Erich Siebenstich ml., Juraj Macho, Patrik Kardos, Zdeno Forgac, Michal Pomsar, Peter Fuchs, Marcel Sramko, Miro Uhliar, Roman Rummel, Vlado Zvonar, Roman Smahovsky, Vlado Fatura, Vlado Koco, Rene Parak, Marcel Pesko, Miro Petrech

**`femalePlayers` (16 entries)** — Same structure with `gender: 'WOMEN'`:
Name collision handling:
- Danka Kleinova: `danka@batl`, Danka Peskova: `danka.p@batl`

Order: Dominika Zaprazna, Simona Nestarcova, Lucia Siebenstichova, Bianka Kestlerova, Augusta Tobiasova, Karolina Strakova, Danka Kleinova, Milka Kraskova, Daska Sojkova, Henrieta Absolonova, Kika Gardonova, Zuzka Oravkinova, Lenka Cubonova, Denisa Faturova, Hela Motlova, Danka Peskova

**`mixedPairs` (18 entries)** — Array of objects with:
- `maleKey`: The male player's email (used to look up the created player)
- `femaleKey`: The female player's email (used to look up the created player)

Pairs from the data file (order matters):
1. tomas@batl + dominika@batl (Zaprazny/Zaprazna)
2. laco@batl + simona@batl (Stevko/Nestarcova)
3. erich@batl + lucia@batl (Siebenstich/Siebenstichova)
4. juraj@batl + bianka@batl (Macho/Kestlerova)
5. patrik@batl + augusta@batl (Kardos/Tobiasova)
6. zdeno@batl + karolina@batl (Forgac/Strakova)
7. michal@batl + milka@batl (Pomsar/Kraskova)
8. peter@batl + henrieta@batl (Fuchs/Absolonova)
9. marcel@batl + zuzka@batl (Sramko/Oravkinova)
10. miro@batl + kika@batl (Uhliar/Gardonova)
11. roman@batl + lenka@batl (Rummel/Cubonova)
12. vlado@batl + danka@batl (Zvonar/Kleinova)
13. roman.s@batl + daska@batl (Smahovsky/Sojkova)
14. vlado.f@batl + denisa@batl (Fatura/Faturova)
15. vlado.k@batl + hela@batl (Koco/Motlova)
16. rene@batl + danka@batl (Parak/Kleinova) — NOTE: Kleinova appears in TWO pairs
17. marcel.p@batl + danka.p@batl (Pesko/Peskova)
18. miro.p@batl + daska@batl (Petrech/Sojkova) — NOTE: Sojkova appears in TWO pairs

**`accountUsers` (2 entries)** — Array describing accounts to create:
```js
[
  { email: 'erich@batl', role: 'ADMIN', password: 'Erich123!' },
  { email: 'rene@batl', role: 'ORGANIZER', password: 'Rene123!' }
]
```

All arrays use `export const`. Use ES module syntax (`export const ...`).
  </action>
  <verify>
    <automated>node -e "import('./backend/prisma/data/players.js').then(m => { const ok = m.malePlayers.length === 18 && m.femalePlayers.length === 16 && m.mixedPairs.length === 18 && m.accountUsers.length === 2; console.log(ok ? 'PASS' : 'FAIL', {male: m.malePlayers.length, female: m.femalePlayers.length, pairs: m.mixedPairs.length, accounts: m.accountUsers.length}); if(!ok) process.exit(1); })"</automated>
  </verify>
  <done>Data file exports 18 male players, 16 female players, 18 mixed pairs, and 2 account definitions. All names match the source data file. Email collision rules are applied correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite seed.js to use real player data with ProSet location</name>
  <files>backend/prisma/seed.js</files>
  <action>
Rewrite `backend/prisma/seed.js` using the data from `backend/prisma/data/players.js`. The script structure should follow the same section pattern but with real data. Key changes:

**Section 1 — USERS (keep existing + add 2 real):**
- KEEP all 5 existing test users EXACTLY as-is (admin@batl.example.com, organizer@batl.example.com, organizer2@batl.example.com, player@batl.example.com, player2@batl.example.com) with same passwords and roles. This is CRITICAL for ACCT-03.
- ADD 2 new users from `accountUsers`: Erich (ADMIN) and Rene (ORGANIZER). Use bcrypt hash with SALT_ROUNDS=12. Upsert by email.
- Store all created users in a map keyed by email for later reference.

**Section 2 — PLAYER PROFILES (keep existing 13 + add 34 real):**
- KEEP all 13 existing generic player profiles EXACTLY as-is (Alex Player, John Smith, Jane Doe, etc.) for PLAY-03 test coverage.
- ADD 34 real players by iterating over `malePlayers` and `femalePlayers` from the data file.
- For each real player: `prisma.playerProfile.upsert()` using email as unique key.
- For Erich and Rene: set `userId` to their respective created user IDs. All other real players: `userId: null`.
- `createdBy` for real players: use the organizer user (organizer@batl.example.com).
- Phone numbers for real players: generate sequential fake numbers (e.g., `+421900000001` incrementing).
- Store all created profiles in a map keyed by email for pair creation.

**Section 3 — CATEGORIES:**
- KEEP all 10 existing categories unchanged. The existing list already includes "Mixed Doubles Open" (DOUBLES, ALL_AGES, MIXED).

**Section 4 — LOCATIONS (replace all with ProSet):**
- DELETE the 4 generic locations (Central Tennis Club, Riverside, Northside, Westside).
- Instead: Create exactly ONE location: `{ clubName: 'ProSet', address: 'Bratislava, Slovakia' }`.
- Use `prisma.location.upsert()` or findFirst+create pattern, keyed on clubName.
- Store the ProSet location ID for tournament references.

**Section 5 — ORGANIZER PROFILES:**
- KEEP existing 2 organizer profiles (Tournament Director, Assistant Director) for the generic organizer accounts.
- ADD organizer profiles for Erich and Rene, linked to their user accounts. Use their real names.

**Section 10 — DOUBLES PAIRS (rewrite):**
- KEEP existing men's doubles pairs (4 pairs) and women's doubles pairs (2 pairs) created from generic players. They remain useful for test coverage on non-mixed categories.
- ADD 18 mixed doubles pairs by iterating over `mixedPairs` from the data file.
- For each pair: look up player1Id and player2Id from the profile map using maleKey and femaleKey.
- Use `prisma.doublesPair.create()` with `categoryId` = Mixed Doubles Open category ID.
- Set `seedingScore: 0` for all mixed pairs (no tournaments played yet, per TOURN-02).
- IMPORTANT: Some players appear in multiple pairs (Kleinova in pairs 12 and 16; Sojkova in pairs 13 and 18). The DoublesPair model allows this — there is no unique constraint preventing a player from being in multiple pairs in the same category.

**New Section — CATEGORY REGISTRATIONS for Mixed Doubles Open (TOURN-01):**
- After creating all real player profiles, register ALL 34 real players in the "Mixed Doubles Open" category using `prisma.categoryRegistration.create()` or upsert with `{ playerId, categoryId }` unique key.
- Status: `ACTIVE`.

**All tournament and ranking sections** (sections 6, 7, 8, 9, 11, 12, 13, 14) should remain as-is for now — they will be rewritten in Plan 02. However, update ALL `locationId` references in tournament creation to use the single ProSet location instead of the old `createdLocations[0]`, `createdLocations[1]`, etc. Also update `backupLocationId` references — either set to null or use the same ProSet location.

**Summary section:** Update the console.log summary to reflect the new data counts.

IMPORTANT: The seed script must remain idempotent — running it twice should not fail. Use upsert where possible. For DoublesPair and other models without upsert-friendly unique constraints, use a deleteMany-then-create pattern or findFirst-guard pattern.
  </action>
  <verify>
    <automated>cd D:/Workspace/BATL/backend && npx prisma db push --force-reset && npx prisma db seed 2>&1 | tail -30</automated>
  </verify>
  <done>
Running `npx prisma db seed` succeeds without errors. Output shows:
- 5 generic + 2 real user accounts created (7 total)
- 13 generic + 34 real player profiles (47 total)
- 10 categories including Mixed Doubles Open
- 1 location: ProSet
- 18 mixed doubles pairs in Mixed Doubles Open
- 34 real players registered in Mixed Doubles Open category
- Existing generic players retained
  </done>
</task>

</tasks>

<verification>
1. Run `npx prisma db push --force-reset && npx prisma db seed` — should complete without errors
2. Query players: `npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM \"PlayerProfile\""` — should show 47 (13 generic + 34 real)
3. Query users: `npx prisma db execute --stdin <<< "SELECT email, role FROM \"User\" WHERE email IN ('erich@batl', 'rene@batl')"` — should show ADMIN and ORGANIZER
4. Query locations: `npx prisma db execute --stdin <<< "SELECT \"clubName\" FROM \"Location\""` — should show only "ProSet"
5. Query mixed pairs: `npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM \"DoublesPair\" dp JOIN \"Category\" c ON dp.\"categoryId\" = c.id WHERE c.name = 'Mixed Doubles Open'"` — should show 18
6. Query category registrations: `npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM \"CategoryRegistration\" cr JOIN \"Category\" c ON cr.\"categoryId\" = c.id WHERE c.name = 'Mixed Doubles Open'"` — should show 34
</verification>

<success_criteria>
- Data file contains all 34 real players with correct names and email conventions
- seed.js imports data file and creates all real players alongside retained generic players
- Erich has ADMIN account, Rene has ORGANIZER account, both linked to player profiles
- All 5 generic accounts unchanged (admin@batl.example.com, etc.)
- Only "ProSet" location exists — no generic location names
- 18 mixed doubles pairs created in Mixed Doubles Open category
- All 34 real players registered in Mixed Doubles Open category
- Seed script runs idempotently without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-real-player-and-league-data/09-01-SUMMARY.md`
</output>
