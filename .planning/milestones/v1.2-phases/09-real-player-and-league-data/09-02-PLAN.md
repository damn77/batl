---
phase: 09-real-player-and-league-data
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - backend/prisma/seed.js
  - backend/prisma/seed-knockout-test.js
autonomous: true
requirements:
  - TOURN-02
  - TOURN-03
  - TOURN-04
  - PAIR-02

must_haves:
  truths:
    - "No tournament exists in Mixed Doubles Open category — rankings show 0 points"
    - "Tournaments exist in age-specific categories (35+, 40+, 50+) with varied statuses"
    - "All tournaments show ProSet as their location"
    - "Tournament statuses include a mix of SCHEDULED, IN_PROGRESS, and COMPLETED"
    - "Existing men's and women's doubles pairs reference real players where applicable"
    - "seed-knockout-test.js uses ProSet location"
  artifacts:
    - path: "backend/prisma/seed.js"
      provides: "Rewritten tournament, ranking, and registration sections"
      contains: "ProSet"
    - path: "backend/prisma/seed-knockout-test.js"
      provides: "Updated knockout test seed with ProSet location"
      contains: "ProSet"
  key_links:
    - from: "backend/prisma/seed.js"
      to: "prisma.tournament.create"
      via: "tournament creation with ProSet locationId"
      pattern: "ProSet.*locationId|locationId.*proSet"
    - from: "backend/prisma/seed-knockout-test.js"
      to: "prisma.location.findFirst"
      via: "finds ProSet location by clubName"
      pattern: "ProSet"
---

<objective>
Rewrite the tournament, registration, and ranking sections of seed.js to use age-specific categories with ProSet location, and update seed-knockout-test.js.

Purpose: Replace generic tournament names and locations with realistic ProSet-hosted tournaments in age-specific categories (35+, 40+, 50+), ensure Mixed Doubles Open has no tournament (rankings at zero), and update the knockout test seed to use ProSet.

Output: A complete seed.js with all sections finalized, and an updated seed-knockout-test.js referencing ProSet.
</objective>

<execution_context>
@C:/Users/erich/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/erich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-real-player-and-league-data/09-CONTEXT.md
@.planning/phases/09-real-player-and-league-data/09-01-SUMMARY.md
@backend/prisma/seed.js
@backend/prisma/seed-knockout-test.js
@backend/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite tournament, registration, and ranking sections of seed.js</name>
  <files>backend/prisma/seed.js</files>
  <action>
Read the current seed.js (as modified by Plan 01) and rewrite the tournament-related sections (original sections 6, 7, 8, 9, 11, 12, 13, 14). All other sections from Plan 01 remain untouched.

**Section 6 — TOURNAMENTS (replace all generic tournaments):**

Remove ALL 6 existing generic tournaments (Spring Singles Championship, Women's Summer League, Masters 35+ Swiss, City Championship, Summer Doubles Classic, Winter Open 2024).

Create new tournaments, ALL at ProSet location, organized by one of the organizer profiles. Distribute across age-specific categories with mixed statuses per TOURN-03 and TOURN-04:

Suggested tournament set (Claude has discretion on exact names/counts/formats):
1. A COMPLETED knockout tournament in Men's Singles 35+ (e.g., "ProSet 35+ Autumn Cup 2025")
2. An IN_PROGRESS knockout tournament in Men's Singles 40+ (e.g., "ProSet 40+ Spring Open 2026")
3. A SCHEDULED knockout tournament in Men's Singles 50+ (e.g., "ProSet 50+ Summer Classic 2026")
4. A COMPLETED knockout tournament in Men's Singles Open (e.g., "ProSet Open Winter Championship 2025")
5. A SCHEDULED tournament in Women's Singles Open (e.g., "ProSet Women's Spring Cup 2026")
6. An IN_PROGRESS tournament in Men's Doubles Open (e.g., "ProSet Men's Doubles Spring 2026")

CRITICAL: Do NOT create any tournament in the "Mixed Doubles Open" category (TOURN-02). The Mixed Doubles Open rankings must start at zero.

Use relative dates (now, lastMonth, nextMonth patterns from the original script) for tournament dates. Each tournament needs: name, categoryId, description, locationId (ProSet), organizerId, capacity, startDate, endDate, registrationOpenDate, registrationCloseDate, status, formatType, formatConfig, defaultScoringRules.

**Section 7 — TOURNAMENT REGISTRATIONS:**
Register appropriate players for each tournament. For age-specific tournaments, register only players whose DOBs make them eligible:
- 35+ tournaments: Register real players born 1991 or earlier (age >= 35 in 2026)
- 40+ tournaments: Register real players born 1986 or earlier
- 50+ tournaments: Register real players born 1976 or earlier
- Open tournaments: Register a mix of generic and real players

For the COMPLETED and IN_PROGRESS tournaments, register enough players for a viable bracket (8-16).

**Section 8 — GROUPS (if needed):**
If any tournament uses GROUP or COMBINED format, create group data. Otherwise this section can be simplified or removed if all tournaments are KNOCKOUT format.

**Section 9 — RANKINGS:**
- KEEP Men's Singles Open SINGLES ranking with generic players (or update to mix generic + real players).
- KEEP Women's Singles Open SINGLES ranking.
- KEEP Men's Singles 35+ SINGLES ranking.
- For Mixed Doubles Open: Create a PAIR ranking with all 18 mixed pairs, each having `totalPoints: 0`, `tournamentCount: 0`, `seedingScore: 0`. This satisfies the success criteria that "Mixed Doubles Open rankings show 0 tournament points."
- Also create MEN and WOMEN individual rankings for Mixed Doubles Open with 0 points for each player.

**Section 11 — PAIR REGISTRATIONS:**
If there's a men's doubles tournament, register the existing men's doubles pairs for it.

**Section 12 — PAIR RANKINGS:**
Keep existing pair ranking logic for men's doubles and women's doubles. The mixed doubles PAIR ranking is handled in section 9 above.

**Section 13 — POINT TABLES:**
Keep point tables section EXACTLY as-is (deleteMany + create pattern).

**Section 14 — PHASE 9 SEED DATA:**
Remove the "Phase 9 seed data" section (completed tournament + archived rankings + tiebreaker scenarios). These were test scaffolding from a prior phase. The new realistic tournaments created in section 6 replace this.

**PAIR-02 — Update existing doubles pairs to use real players:**
The existing men's doubles pairs (4 pairs from generic players) and women's doubles pairs (2 pairs) should be updated to use real players where applicable. Instead of pairing "Alex Player + John Smith", pair real male players. Instead of "Jane Doe + Sarah Williams", pair real female players. Keep the same seeding scores (these represent existing tournament history). This makes the non-mixed doubles categories feel real too.

**Summary section:** Update the final console.log summary to accurately reflect all data.
  </action>
  <verify>
    <automated>cd D:/Workspace/BATL/backend && npx prisma db push --force-reset && npx prisma db seed 2>&1 | tail -40</automated>
  </verify>
  <done>
Running `npx prisma db seed` succeeds. Output confirms:
- Multiple tournaments across age-specific categories (35+, 40+, 50+)
- Mix of SCHEDULED, IN_PROGRESS, and COMPLETED statuses
- All tournaments at ProSet location
- No tournament in Mixed Doubles Open
- Mixed Doubles Open pair ranking exists with 0 points for all 18 pairs
- Men's/women's doubles pairs now reference real player names
  </done>
</task>

<task type="auto">
  <name>Task 2: Update seed-knockout-test.js to use ProSet location</name>
  <files>backend/prisma/seed-knockout-test.js</files>
  <action>
Update `backend/prisma/seed-knockout-test.js` to find and use the ProSet location instead of `findFirst()` which could find any location.

Changes:
1. Replace `const location = await prisma.location.findFirst()` with `const location = await prisma.location.findFirst({ where: { clubName: 'ProSet' } })`.
2. Update the error message to reference ProSet: `'No ProSet location found. Run main seed first.'`
3. The rest of the script can remain as-is — it finds male players by gender which still works since real players have the same gender field structure.

This is a minimal change. The knockout test seed already queries for an organizer, category, location, and players generically. The only change is making the location lookup explicit.
  </action>
  <verify>
    <automated>cd D:/Workspace/BATL/backend && npx prisma db push --force-reset && npx prisma db seed && node prisma/seed-knockout-test.js 2>&1 | tail -20</automated>
  </verify>
  <done>
seed-knockout-test.js runs successfully after main seed, finds ProSet location, creates knockout tournament test data. No generic location names referenced.
  </done>
</task>

</tasks>

<verification>
1. Full seed pipeline: `npx prisma db push --force-reset && npx prisma db seed && node prisma/seed-knockout-test.js` — all succeed
2. Query tournaments: verify none exist in Mixed Doubles Open category
3. Query tournaments: verify all have ProSet as location (no "Central Tennis Club", "Riverside", etc.)
4. Query tournament statuses: verify SCHEDULED, IN_PROGRESS, and COMPLETED all present
5. Query Mixed Doubles Open ranking entries: verify all 18 pairs have 0 total points
6. Start backend server and verify app shows tournaments with ProSet location
</verification>

<success_criteria>
- No tournament exists in Mixed Doubles Open category
- Tournaments span age-specific categories (35+, 40+, 50+) plus Open
- All 3 statuses present: SCHEDULED, IN_PROGRESS, COMPLETED
- Every tournament location is "ProSet"
- Mixed Doubles Open rankings show 0 points for all 18 pairs
- Men's/women's doubles pairs updated to use real player names
- seed-knockout-test.js explicitly finds ProSet location
- Full seed pipeline runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-real-player-and-league-data/09-02-SUMMARY.md`
</output>
