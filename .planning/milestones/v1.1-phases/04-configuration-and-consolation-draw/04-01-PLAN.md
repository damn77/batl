---
phase: 04-configuration-and-consolation-draw
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/KnockoutConfigPanel.jsx
  - frontend/src/pages/TournamentRulesSetupPage.jsx
autonomous: true
requirements:
  - CONF-01

must_haves:
  truths:
    - "Organizer sees three options in the Match Guarantee dropdown: Single Elimination, Double Elimination (2 matches), and a disabled Until Placement (coming soon)"
    - "New knockout tournaments default to Double Elimination (MATCH_2)"
    - "Match Guarantee dropdown is locked (disabled) when tournament status is IN_PROGRESS or COMPLETED"
    - "Existing tournaments without matchGuarantee in formatConfig display correctly (backwards-compatible)"
  artifacts:
    - path: "frontend/src/components/KnockoutConfigPanel.jsx"
      provides: "Updated dropdown with three options; MATCH_3 removed"
      contains: "Until Placement"
    - path: "frontend/src/pages/TournamentRulesSetupPage.jsx"
      provides: "Default matchGuarantee changed from MATCH_1 to MATCH_2"
      contains: "MATCH_2"
  key_links:
    - from: "frontend/src/pages/TournamentRulesSetupPage.jsx"
      to: "frontend/src/components/KnockoutConfigPanel.jsx"
      via: "FormatConfigPanel renders KnockoutConfigPanel with disabled prop"
      pattern: "disabled.*IN_PROGRESS|IN_PROGRESS.*disabled"
---

<objective>
Update the Match Guarantee configuration UI for knockout tournaments so organizers can choose between Single Elimination and Double Elimination, with Double Elimination as the default for new tournaments.

Purpose: CONF-01 — Organizer must be able to set Match Guarantee when creating or editing a knockout tournament.
Output: Updated KnockoutConfigPanel.jsx with correct options and TournamentRulesSetupPage.jsx with MATCH_2 as default. The "Until Placement" option is shown greyed-out/disabled to signal the coming-soon roadmap item.
</objective>

<execution_context>
@C:/Users/erich/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/erich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From frontend/src/components/KnockoutConfigPanel.jsx (current state):
```jsx
// Props:
// value = { matchGuarantee: 'MATCH_1' | 'MATCH_2' | 'MATCH_3' }
// onChange: (newValue) => void
// disabled: boolean

function KnockoutConfigPanel({ value = { matchGuarantee: 'MATCH_1' }, onChange, disabled = false })
// Currently has three <option> elements: MATCH_1, MATCH_2, MATCH_3
// Has Form.Text help text that changes per value
```

From frontend/src/pages/TournamentRulesSetupPage.jsx (current state):
```jsx
// Line 24-27 — initial formatConfig state:
const [formatConfig, setFormatConfig] = useState({
  formatType: 'KNOCKOUT',
  matchGuarantee: 'MATCH_1'   // <-- must change to 'MATCH_2'
});

// Line 258-265 — FormatConfigPanel usage:
<FormatConfigPanel
  formatType={formatType}
  formatConfig={formatConfig}
  onChange={(newConfig) => setFormatConfig(newConfig)}
  disabled={hasMatches}           // <-- currently disabled when hasMatches
  playerCount={registrationStats.registered}
/>
```

From backend (for reference — tournament status values):
```
SCHEDULED  → tournament created, registration open/closed, draw not yet started
IN_PROGRESS → startTournament() called, matches underway
COMPLETED   → all matches done
CANCELLED
```

The lock condition per user decisions: "locked when tournament status becomes ACTIVE (i.e., when the draw is executed and the tournament moves to ACTIVE)". In DB terms: lock when status === 'IN_PROGRESS' (or 'COMPLETED'). The TournamentRulesSetupPage currently receives the tournament object; it loads status via getTournamentFormat().

Note: `getTournamentFormat()` returns `{ name, formatType, formatConfig, defaultScoringRules, ... }` — it does NOT currently return tournament status. The page uses `hasMatches` to control disabled state. Check what data is available; if tournament status is not available on this page, disable the Match Guarantee field when `hasMatches === true` (consistent with existing behavior: once matches exist the draw has run and the tournament is effectively locked).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update KnockoutConfigPanel dropdown options and default</name>
  <files>frontend/src/components/KnockoutConfigPanel.jsx</files>
  <action>
    Replace the current three-option dropdown with the new three options per user decisions:

    1. Remove the MATCH_3 option entirely.
    2. Replace options with:
       - value="MATCH_1": label "Single Elimination"
       - value="MATCH_2": label "Double Elimination (2 matches)"
       - A third `<option>` with value="" disabled: "Until Placement (coming soon)" — render it as `<option value="UNTIL_PLACEMENT" disabled>Until Placement (coming soon)</option>`. Use `className="text-muted"` for visual distinction. This option is always non-selectable.
    3. Remove the `<Form.Text>` help text block entirely (user decision: no help text below the dropdown).
    4. Change the default prop to `{ matchGuarantee: 'MATCH_2' }` (new tournaments default to Double Elimination).
    5. Change the `value={value.matchGuarantee || 'MATCH_1'}` to `value={value.matchGuarantee || 'MATCH_2'}`.
    6. Update the JSDoc comment to reflect the new valid values: `'MATCH_1' | 'MATCH_2'`.

    Result: A two-selectable-option dropdown (MATCH_1, MATCH_2) with a greyed-out disabled third option for Until Placement, no help text.
  </action>
  <verify>
    Manual visual check: `npm run dev` in frontend, navigate to any knockout tournament rules setup — dropdown shows exactly three rows, third is disabled, no help text visible.
    Automated: `grep -n "MATCH_3\|Form.Text" frontend/src/components/KnockoutConfigPanel.jsx` returns no results.
  </verify>
  <done>
    KnockoutConfigPanel.jsx has no MATCH_3 option, no Form.Text, contains "Until Placement (coming soon)" as a disabled option, default prop is MATCH_2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Change TournamentRulesSetupPage default to MATCH_2 and lock Match Guarantee when IN_PROGRESS</name>
  <files>frontend/src/pages/TournamentRulesSetupPage.jsx</files>
  <action>
    Two changes to TournamentRulesSetupPage.jsx:

    **Change 1 — Default formatConfig to MATCH_2:**
    Line 24-27: Change initial state from `matchGuarantee: 'MATCH_1'` to `matchGuarantee: 'MATCH_2'`.

    **Change 2 — Lock Match Guarantee field when IN_PROGRESS:**
    The page already has a `hasMatches` boolean that disables the format selectors when matches have been created (i.e., the draw has been run). The Match Guarantee lock condition is the same: once a draw exists / tournament is running, the field must be locked.

    Check if the API response from `getTournamentFormat()` already returns tournament status. Open `frontend/src/services/tournamentRulesService.js` and check the response shape. If status is returned, extract it and add a separate `isLocked` state: `const isLocked = tournament.status === 'IN_PROGRESS' || tournament.status === 'COMPLETED' || hasMatches`.

    If status is NOT returned by the API, use `hasMatches` alone as the lock signal for the Match Guarantee field (this is functionally equivalent: the draw generates matches, which sets hasMatches=true, which is when the lock kicks in).

    Either way: pass `disabled={hasMatches}` to the FormatConfigPanel (which already does this). No code change may be needed for the lock — verify the existing `disabled={hasMatches}` on `<FormatConfigPanel>` already covers the lock requirement. If it does, this change only needs the default value update.

    Document your finding in a comment: `// Match Guarantee locked via hasMatches (true once draw generated)`.
  </action>
  <verify>
    `grep -n "matchGuarantee" frontend/src/pages/TournamentRulesSetupPage.jsx` shows `'MATCH_2'` (not `'MATCH_1'`).
  </verify>
  <done>
    TournamentRulesSetupPage.jsx initializes formatConfig with matchGuarantee: 'MATCH_2'. Existing disabled={hasMatches} on FormatConfigPanel covers the lock requirement.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "MATCH_3\|Form.Text" frontend/src/components/KnockoutConfigPanel.jsx` returns nothing
2. `grep -n "matchGuarantee" frontend/src/pages/TournamentRulesSetupPage.jsx` shows MATCH_2
3. `grep -n "Until Placement" frontend/src/components/KnockoutConfigPanel.jsx` shows the disabled option
</verification>

<success_criteria>
- KnockoutConfigPanel has exactly 2 selectable options (MATCH_1, MATCH_2) plus 1 disabled "Until Placement (coming soon)" option
- MATCH_3 is completely removed from KnockoutConfigPanel
- No Form.Text help text in KnockoutConfigPanel
- Default for new knockout tournaments is MATCH_2
- FormatConfigPanel disabled={hasMatches} already locks the field after draw execution
</success_criteria>

<output>
After completion, create `.planning/phases/04-configuration-and-consolation-draw/04-01-SUMMARY.md`
</output>
