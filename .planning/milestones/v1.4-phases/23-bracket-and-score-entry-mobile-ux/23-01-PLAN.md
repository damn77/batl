---
phase: 23-bracket-and-score-entry-mobile-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/KnockoutBracket.css
autonomous: true
requirements: [BRKT-01, BRKT-02, BRKT-03, BRKT-04]

must_haves:
  truths:
    - "Bracket zoom +/- buttons are 44px minimum tap target on mobile (<576px)"
    - "Reset and BYE toggle buttons are 44px minimum tap target on mobile"
    - "Bracket controls remain in a single horizontal row on mobile with wrapping only for My Match"
    - "Touch drag panning still works on real mobile (existing quick-6 fix preserved)"
    - "Bracket loads at 50% zoom on mobile showing 2-3 rounds (existing behavior preserved)"
  artifacts:
    - path: "frontend/src/components/KnockoutBracket.css"
      provides: "44px mobile tap target rules for bracket controls"
      contains: "min-height: 44px"
  key_links:
    - from: "frontend/src/components/KnockoutBracket.css"
      to: "frontend/src/components/BracketControls.jsx"
      via: "CSS class .bracket-controls targeting ButtonGroup buttons"
      pattern: "\\.bracket-controls.*btn"
---

<objective>
Add 44px minimum tap targets to all bracket control buttons on mobile viewports.

Purpose: Bracket zoom, reset, and BYE toggle buttons are currently size="sm" Bootstrap buttons (~31px height), too small for reliable finger tapping on mobile. This plan adds CSS overrides at the sm breakpoint to enforce 44px minimum height without changing desktop appearance.

Output: Updated KnockoutBracket.css with mobile tap target rules for bracket controls.
</objective>

<execution_context>
@C:/Users/erich/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/erich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-bracket-and-score-entry-mobile-ux/23-CONTEXT.md
@frontend/src/components/BRACKET-VIEW-GUIDE.md
@frontend/src/components/KnockoutBracket.css
@frontend/src/components/BracketControls.jsx

<interfaces>
<!-- BracketControls renders a .bracket-controls div containing:
     - ButtonGroup size="sm" with zoom out (-), percentage display, zoom in (+) buttons
     - Reset button (size="sm", variant="outline-primary")
     - BYE toggle button (size="sm", variant="warning"/"outline-warning")
     All buttons use Bootstrap .btn.btn-sm classes.

     The .bracket-controls-wrapper div is the sticky parent container.
     The .bracket-controls div is a direct child with d-flex gap-2.

     BracketMyMatch component renders inside .bracket-controls-wrapper as a sibling to BracketControls. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 44px mobile tap targets for bracket controls</name>
  <files>frontend/src/components/KnockoutBracket.css</files>
  <action>
Add CSS rules inside the existing `@media (max-width: 576px)` block in KnockoutBracket.css to enforce 44px minimum tap targets on bracket control buttons. Specifically:

1. Target `.bracket-controls .btn` and set `min-height: 44px; min-width: 44px; font-size: 1rem;` so all bracket control buttons (zoom in/out, percentage, reset, BYE toggle) meet mobile tap target requirements.

2. Target `.bracket-controls .btn-group .btn` specifically to ensure the ButtonGroup buttons (zoom controls) also get 44px height — ButtonGroup can override individual button sizing.

3. Change the existing `.bracket-controls-wrapper` flex-direction from `column` to `row` with `flex-wrap: wrap` at the 576px breakpoint. The current `flex-direction: column` stacks controls vertically which wastes space. A horizontal row with wrapping is preferred per user decision. Keep `justify-content: center`.

4. Keep `.bracket-controls` with `flex-wrap: wrap; justify-content: center;` (already exists).

5. Ensure `.bracket-my-match .btn` also gets `min-height: 44px` for the My Match button.

Do NOT change any non-mobile CSS. Do NOT touch viewport, touch-action, or pan-related styles. Do NOT modify BracketControls.jsx — all changes are CSS-only.

Why CSS-only: The buttons already render correctly via React Bootstrap. We only need size overrides at the mobile breakpoint. Changing JSX props would affect desktop too.
  </action>
  <verify>
    <automated>cd D:/Workspace/BATL && node -e "const fs=require('fs'); const css=fs.readFileSync('frontend/src/components/KnockoutBracket.css','utf8'); const has44=css.includes('min-height: 44px'); const hasTouch=css.includes('touch-action: none'); const noColumn=!css.match(/@media[^}]*max-width:\s*576px[^}]*flex-direction:\s*column/s); console.log('44px tap targets:', has44); console.log('touch-action preserved:', hasTouch); if(!has44) process.exit(1);"</automated>
  </verify>
  <done>
    - All bracket control buttons (.bracket-controls .btn) have min-height: 44px at max-width: 576px
    - Controls layout is horizontal row with wrapping on mobile, not vertical column
    - Desktop appearance is unchanged (size="sm" buttons remain compact)
    - All existing bracket CSS invariants preserved (touch-action: none, overflow: hidden, etc.)
  </done>
</task>

</tasks>

<verification>
- Open bracket view in Chrome DevTools at 375px width
- Inspect zoom +/- buttons: min-height should be 44px
- Inspect Reset button: min-height should be 44px
- Inspect BYE toggle button: min-height should be 44px
- Controls should be in a horizontal row, wrapping if needed
- At desktop width (1200px), buttons should remain size="sm" (compact)
- Touch drag still works (no CSS changes to viewport or touch-action)
</verification>

<success_criteria>
All bracket control buttons meet 44px minimum tap target on mobile (<576px viewport) while desktop UI remains unchanged. Bracket touch panning and zoom behavior preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/23-bracket-and-score-entry-mobile-ux/23-01-SUMMARY.md`
</output>
