---
phase: 26-player-profile-mobile-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/PlayerPublicProfilePage.jsx
  - frontend/src/components/MatchHistoryTab.jsx
autonomous: true
requirements:
  - RESP-06
gap_closure: true

must_haves:
  truths:
    - "Player profile page has no horizontal overflow at 375px viewport width"
    - "Save/cancel buttons in edit mode wrap properly on 375px viewport"
    - "Profile field spacing is tighter on mobile for density"
    - "Match history is readable on mobile without horizontal scrolling"
  artifacts:
    - path: "frontend/src/pages/PlayerPublicProfilePage.jsx"
      provides: "Mobile-optimized player profile with flex-wrap buttons and tighter spacing"
      contains: "flex-wrap"
    - path: "frontend/src/components/MatchHistoryTab.jsx"
      provides: "Mobile-friendly match history with column hiding or card layout"
      contains: "d-none d-sm"
  key_links:
    - from: "frontend/src/pages/PlayerPublicProfilePage.jsx"
      to: "frontend/src/components/MatchHistoryTab.jsx"
      via: "Tab.Pane rendering MatchHistoryTab component"
      pattern: "MatchHistoryTab"
---

<objective>
Apply mobile optimizations to the live PlayerPublicProfilePage.jsx (and its MatchHistoryTab child) to close the RESP-06 gap. Phase 25-03 accidentally applied these patterns to the dead PlayerProfilePage.jsx file.

Purpose: Close the last remaining v1.4 requirement gap (RESP-06) so the player profile page is fully usable at 375px viewport width.
Output: Mobile-optimized PlayerPublicProfilePage.jsx with flex-wrap buttons, tighter spacing, and mobile-friendly match history.
</objective>

<execution_context>
@C:/Users/erich/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/erich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.4-MILESTONE-AUDIT.md

@frontend/src/pages/PlayerPublicProfilePage.jsx
@frontend/src/components/MatchHistoryTab.jsx
@frontend/src/pages/PlayerProfilePage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile-optimize PlayerPublicProfilePage</name>
  <files>frontend/src/pages/PlayerPublicProfilePage.jsx</files>
  <action>
Apply the following mobile optimizations to PlayerPublicProfilePage.jsx (the LIVE profile page at /players/:id):

1. **Edit mode button wrap** (renderEditForm function, ~line 265): Change `d-flex gap-2` to `d-flex flex-wrap gap-2` on the save/cancel button container so buttons stack on narrow screens.

2. **Tighten view-mode field spacing** (renderProfileView function): In the profile view fields, reduce spacing for mobile density:
   - Change the field wrapper divs from `mb-3` to `mb-2` in renderProfileView (both isOwnProfile and public view sections)
   - Change `fs-5` on field value `<p>` elements to `fs-6` for tighter mobile density (fs-5 is too large on 375px)

3. **Page title responsive sizing**: On the `<h2 className="mb-4">` heading (~line 295), add `fs-4` class to prevent overly large heading on mobile: `<h2 className="mb-3 fs-4">`

Do NOT change the Tab-based layout structure — it works well on mobile. Do NOT add an Accordion wrapper — the dead file's accordion pattern was a placeholder; the live file's tab structure is better UX.

Do NOT touch edit form field spacing (mb-3 is correct for form inputs with validation feedback — tighter spacing makes forms hard to use).
  </action>
  <verify>
    <automated>cd D:/Workspace/BATL && node -e "const fs=require('fs'); const c=fs.readFileSync('frontend/src/pages/PlayerPublicProfilePage.jsx','utf8'); const checks=[c.includes('flex-wrap'), c.includes('fs-6'), c.includes('mb-2'), c.includes('fs-4')]; console.log('flex-wrap:',checks[0],'fs-6:',checks[1],'mb-2:',checks[2],'fs-4:',checks[3]); if(!checks.every(Boolean)) process.exit(1); console.log('ALL PASS');"</automated>
  </verify>
  <done>PlayerPublicProfilePage has flex-wrap on edit buttons, tighter mb-2 spacing in view mode, and fs-4/fs-6 for mobile-appropriate text sizing</done>
</task>

<task type="auto">
  <name>Task 2: Mobile-optimize MatchHistoryTab</name>
  <files>frontend/src/components/MatchHistoryTab.jsx</files>
  <action>
Apply mobile optimizations to MatchHistoryTab.jsx so the match history table does not overflow at 375px:

1. **Hide non-essential columns on mobile**: Add `d-none d-sm-table-cell` class to the Category and Date columns (both `<th>` and `<td>` elements) so they are hidden below 576px. This follows the established Phase 25 pattern (RESP-02).
   - Category th (~line 192): add `className="d-none d-sm-table-cell"`
   - Category td (~line 205): add `className="d-none d-sm-table-cell"`
   - Date th (~line 189): add `className="d-none d-sm-table-cell"` (keep onClick and style)
   - Date td (~line 204): add `className="d-none d-sm-table-cell"`

2. **Category filter responsive layout** (~line 144): Change the filter container from `d-flex justify-content-end align-items-center mb-3` to `d-flex flex-wrap justify-content-end align-items-center gap-2 mb-3`. Also change the Form.Select inline style from `{ width: 'auto', minWidth: '180px' }` to `{ width: 'auto', maxWidth: '100%' }` so the dropdown does not overflow on 375px.

3. **Score column compact**: No changes needed — score is already short text.

The 4 remaining visible columns on mobile (Tournament, Opponent, Score, Result) will fit at 375px with the `responsive` prop already on the Table.
  </action>
  <verify>
    <automated>cd D:/Workspace/BATL && node -e "const fs=require('fs'); const c=fs.readFileSync('frontend/src/components/MatchHistoryTab.jsx','utf8'); const checks=[c.includes('d-none d-sm-table-cell'), (c.match(/d-none d-sm-table-cell/g)||[]).length>=4, c.includes('flex-wrap')]; console.log('column-hide:',checks[0],'4-instances:',checks[1],'filter-wrap:',checks[2]); if(!checks.every(Boolean)) process.exit(1); console.log('ALL PASS');"</automated>
  </verify>
  <done>MatchHistoryTab hides Category and Date columns on mobile (below 576px) and filter controls wrap properly on narrow screens</done>
</task>

</tasks>

<verification>
1. Run the automated verify commands for both tasks
2. Build check: `cd D:/Workspace/BATL/frontend && npx vite build --mode development 2>&1 | tail -5` (no build errors)
3. Visual: PlayerPublicProfilePage renders without horizontal overflow at 375px viewport
</verification>

<success_criteria>
- PlayerPublicProfilePage.jsx has flex-wrap on save/cancel buttons, tighter mb-2 spacing in view mode, mobile-appropriate font sizes
- MatchHistoryTab.jsx hides Category and Date columns below 576px
- No horizontal overflow at 375px viewport width on the player profile page
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/26-player-profile-mobile-fix/26-01-SUMMARY.md`
</output>
