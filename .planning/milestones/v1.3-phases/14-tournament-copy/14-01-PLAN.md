---
phase: 14-tournament-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/tournamentService.js
  - backend/src/api/tournamentController.js
  - backend/src/api/routes/tournamentRoutes.js
  - backend/src/api/validators/tournamentCopyValidator.js
  - backend/__tests__/integration/tournamentCopy.test.js
autonomous: true
requirements: [COPY-01, COPY-02, COPY-03, COPY-04]

must_haves:
  truths:
    - "POST /api/v1/tournaments/:id/copy creates a new SCHEDULED tournament from source"
    - "Copied tournament includes category, format, scoring rules, location, capacity, and all auxiliary fields"
    - "Copied tournament has no name, no dates, no registrations, no draw"
    - "Copied tournament has the copying user as organizer"
    - "TournamentPointConfig is cloned if source has one"
    - "Override fields in request body are applied to the copy"
  artifacts:
    - path: "backend/src/services/tournamentService.js"
      provides: "copyTournament service function"
      contains: "copyTournament"
    - path: "backend/src/api/tournamentController.js"
      provides: "copyTournament controller handler"
      contains: "copyTournament"
    - path: "backend/src/api/routes/tournamentRoutes.js"
      provides: "POST /:id/copy route"
      contains: "copy"
    - path: "backend/src/api/validators/tournamentCopyValidator.js"
      provides: "Joi schema for copy request body"
      contains: "copyTournamentSchema"
    - path: "backend/__tests__/integration/tournamentCopy.test.js"
      provides: "Integration tests for copy endpoint"
      min_lines: 80
  key_links:
    - from: "backend/src/api/routes/tournamentRoutes.js"
      to: "backend/src/api/tournamentController.js"
      via: "route handler registration"
      pattern: "copyTournament"
    - from: "backend/src/api/tournamentController.js"
      to: "backend/src/services/tournamentService.js"
      via: "service function call"
      pattern: "tournamentService\\.copyTournament"
---

<objective>
Create the backend API endpoint for tournament copying: POST /api/v1/tournaments/:id/copy

Purpose: Enable organizers to duplicate tournament configuration via API, producing a new SCHEDULED tournament with all settings carried over but no name, dates, registrations, or draw.

Output: Working copy endpoint with validation, service logic, controller, route registration, and integration tests.
</objective>

<execution_context>
@C:/Users/erich/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/erich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tournament-copy/14-CONTEXT.md

@backend/src/services/tournamentService.js
@backend/src/api/tournamentController.js
@backend/src/api/routes/tournamentRoutes.js
@backend/src/api/validators/bracketPersistenceValidator.js
@backend/prisma/schema.prisma

<interfaces>
<!-- Key patterns the executor needs -->

From backend/src/services/tournamentService.js:
```javascript
import createHttpError from 'http-errors';
// Error pattern: throw createHttpError(404, 'message', { code: 'ERROR_CODE' });
export async function createTournament(data, userId) { ... }
```

From backend/src/api/tournamentController.js:
```javascript
export async function createTournament(req, res, next) {
  try {
    const tournament = await tournamentService.createTournament(req.body, req.user.id);
    res.status(201).json({ success: true, data: { tournament } });
  } catch (err) { next(err); }
}
```

From backend/src/api/routes/tournamentRoutes.js:
```javascript
import { isAuthenticated, authorize } from '../../middleware/auth.js';
import { validateBody } from '../../middleware/validate.js';
router.post('/', isAuthenticated, authorize('create', 'Tournament'), validateBody(schemas.tournamentCreation), createTournament);
```

Tournament model key fields to copy:
- categoryId, description, locationId, backupLocationId, courts, capacity
- organizerId, deputyOrganizerId (organizer reset to copier)
- entryFee, rulesUrl, prizeDescription
- registrationOpenDate, registrationCloseDate (NOT copied — dates are empty)
- minParticipants, waitlistDisplayOrder
- formatType, formatConfig, defaultScoringRules

Fields NOT copied (reset):
- name (empty/required in overrides), startDate, endDate (required in overrides or null)
- status = SCHEDULED, registrationClosed = false
- No registrations, no draw, no brackets
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Copy tournament service function, validator, and integration tests</name>
  <files>
    backend/src/api/validators/tournamentCopyValidator.js,
    backend/src/services/tournamentService.js,
    backend/src/api/tournamentController.js,
    backend/src/api/routes/tournamentRoutes.js,
    backend/__tests__/integration/tournamentCopy.test.js
  </files>
  <behavior>
    - Test: POST /tournaments/:id/copy with valid overrides returns 201 with new tournament in SCHEDULED status
    - Test: Copied tournament has source's categoryId, formatConfig, defaultScoringRules, locationId, capacity, courts, entryFee, rulesUrl, prizeDescription, minParticipants, waitlistDisplayOrder, description
    - Test: Copied tournament has NO name (or name from override), NO startDate/endDate (or from override), NO registrations, NO draw
    - Test: Copied tournament organizer is set to the copying user (not source organizer)
    - Test: Override fields in body (name, startDate, endDate, capacity, description) are applied to the copy
    - Test: Source tournament with TournamentPointConfig gets config cloned to new tournament
    - Test: Source tournament without TournamentPointConfig creates copy without config
    - Test: 404 when source tournament ID not found
    - Test: 401 when not authenticated
    - Test: Copy works regardless of source tournament status (SCHEDULED, IN_PROGRESS, COMPLETED)
  </behavior>
  <action>
    1. Create `backend/src/api/validators/tournamentCopyValidator.js`:
       - Joi schema `copyTournamentSchema` accepting optional override fields: name (string), description (string), startDate (ISO date string), endDate (ISO date string), capacity (integer, min 2), clubName (string), address (string). All fields optional since the form sends the full edited payload.

    2. Add `copyTournament(sourceId, overrides, userId)` to `backend/src/services/tournamentService.js`:
       - Fetch source tournament by ID with include: { location: true, backupLocation: true, organizer: true }
       - Throw 404 if not found
       - Build new tournament data object copying: categoryId, description, formatType, formatConfig, defaultScoringRules, locationId, backupLocationId, courts, capacity, entryFee, rulesUrl, prizeDescription, minParticipants, waitlistDisplayOrder, deputyOrganizerId
       - Set organizer to copying user via organizerService.findOrCreateOrganizer(userId)
       - Apply overrides on top (name, dates, capacity, description, location via clubName/address)
       - If overrides include clubName, use locationService.findOrCreateLocation for primary location
       - Set status = SCHEDULED, registrationClosed = false
       - For dates: if overrides provide startDate/endDate use those, otherwise set to null (Prisma will need these — actually Tournament model requires startDate/endDate as non-nullable DateTime. So require name+startDate+endDate in the frontend form before submit, but the API should accept them as overrides. If not provided, throw 400 with code MISSING_REQUIRED_OVERRIDES)
       - Use prisma.$transaction to: (a) create tournament, (b) if source has TournamentPointConfig, create a copy with new tournamentId
       - Return new tournament with includes (category, location, organizer)

    3. Add `copyTournament` handler to `backend/src/api/tournamentController.js`:
       - Extract source id from req.params, overrides from req.body, userId from req.user.id
       - Call tournamentService.copyTournament(id, req.body, req.user.id)
       - Return 201 with { success: true, data: { tournament, copiedFrom: { id, name } } }
       - The copiedFrom field lets the frontend show "Copied from: [name]" confirmation

    4. Register route in `backend/src/api/routes/tournamentRoutes.js`:
       - Add POST `/:id/copy` BEFORE the generic `/:id` PATCH route to avoid path shadowing
       - Use isAuthenticated, authorize('create', 'Tournament'), validateBody(copyTournamentSchema)
       - Import copyTournament from controller and copyTournamentSchema from validator

    5. Write integration tests in `backend/__tests__/integration/tournamentCopy.test.js`:
       - Use existing test patterns (see other integration test files for setup/teardown)
       - Cover all behaviors listed above
  </action>
  <verify>
    <automated>cd D:/Workspace/BATL && npx vitest run backend/__tests__/integration/tournamentCopy.test.js --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <done>
    POST /api/v1/tournaments/:id/copy creates a new SCHEDULED tournament copying all configuration fields, applying overrides, cloning point config, setting copier as organizer, and returning the new tournament with copiedFrom metadata. All integration tests pass.
  </done>
</task>

</tasks>

<verification>
- POST /api/v1/tournaments/:id/copy returns 201 with new tournament
- New tournament has SCHEDULED status, copying user as organizer
- All config fields (category, format, scoring, location, capacity, etc.) are copied
- TournamentPointConfig is cloned when present
- Override fields are applied correctly
- 404 returned for invalid source ID
- Integration tests pass
</verification>

<success_criteria>
- Copy endpoint functional and tested
- All COPY-01 through COPY-04 backend requirements satisfied
- Tests cover happy path, overrides, point config cloning, and error cases
</success_criteria>

<output>
After completion, create `.planning/phases/14-tournament-copy/14-01-SUMMARY.md`
</output>
