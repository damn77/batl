---
phase: 20-mobile-dev-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.js
  - frontend/index.html
autonomous: false
requirements:
  - TOOL-01
  - TOOL-02
must_haves:
  truths:
    - "Running `npm run dev:mobile` in frontend/ prints a QR code in the terminal"
    - "Scanning that QR code on a phone opens the app within 30 seconds"
    - "Running `npm run dev` still works as before (localhost only, no QR code)"
    - "The viewport meta tag includes viewport-fit=cover"
  artifacts:
    - path: "frontend/vite.config.js"
      provides: "vite-plugin-qrcode integration"
      contains: "qrcode"
    - path: "frontend/package.json"
      provides: "dev:mobile npm script"
      contains: "dev:mobile"
    - path: "frontend/index.html"
      provides: "viewport-fit=cover meta tag"
      contains: "viewport-fit=cover"
  key_links:
    - from: "frontend/package.json dev:mobile script"
      to: "vite --host CLI flag"
      via: "npm script definition"
      pattern: "vite --host"
    - from: "vite --host flag"
      to: "vite-plugin-qrcode"
      via: "plugin detects network URL and prints QR"
      pattern: "qrcode"
---

<objective>
Install vite-plugin-qrcode, add a `dev:mobile` npm script that enables network hosting with QR code output, and update the viewport meta tag for device notch support.

Purpose: Enable developers to test the app on a real mobile device by scanning a QR code from the terminal, eliminating the friction of manually typing IP addresses.
Output: Three modified files (vite.config.js, package.json, index.html) providing mobile dev tooling.
</objective>

<execution_context>
@C:/Users/erich/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/erich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-mobile-dev-tooling/20-CONTEXT.md
@.planning/phases/20-mobile-dev-tooling/20-RESEARCH.md

<interfaces>
<!-- Existing files to modify — current state for executor reference -->

From frontend/vite.config.js:
```javascript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3001,
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true
      }
    }
  }
});
```

From frontend/package.json (scripts section):
```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "lint": "eslint src --fix"
}
```

From frontend/index.html (line 5):
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vite-plugin-qrcode, add dev:mobile script, update vite config</name>
  <files>frontend/package.json, frontend/vite.config.js</files>
  <action>
    1. Install vite-plugin-qrcode as a dev dependency:
       `cd frontend && npm install --save-dev vite-plugin-qrcode`

    2. Update frontend/vite.config.js to import and add the qrcode plugin:
       - Add import: `import { qrcode } from 'vite-plugin-qrcode';`
       - Add `qrcode()` to the plugins array after `react()`
       - Do NOT add `server.host` to the config — the CLI flag handles this per user decision

    3. Add `dev:mobile` script to frontend/package.json scripts:
       `"dev:mobile": "vite --host"`
       Place it after the existing `"dev": "vite"` line.
       Do NOT modify the existing `"dev"` script — it must remain `"vite"` (localhost-only).

    Result: `npm run dev` works as before (localhost, no QR). `npm run dev:mobile` binds to 0.0.0.0 and prints a QR code in the terminal.
  </action>
  <verify>
    <automated>cd D:/Workspace/BATL/frontend && node -e "import('vite-plugin-qrcode').then(m => console.log('OK: qrcode export =', typeof m.qrcode)).catch(e => { console.error('FAIL:', e.message); process.exit(1); })" && grep -q "dev:mobile" package.json && echo "OK: dev:mobile script found" && grep -q "qrcode" vite.config.js && echo "OK: qrcode in vite config"</automated>
  </verify>
  <done>vite-plugin-qrcode installed as devDependency, vite.config.js imports and uses qrcode plugin, package.json has dev:mobile script with --host flag, existing dev script unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Add viewport-fit=cover to index.html</name>
  <files>frontend/index.html</files>
  <action>
    Update the viewport meta tag on line 5 of frontend/index.html from:
    `<meta name="viewport" content="width=device-width, initial-scale=1.0" />`
    to:
    `<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />`

    This is a permanent change that applies to all modes (dev and production). It tells the browser to render content edge-to-edge, enabling future safe-area-inset CSS in Phase 25.

    Do NOT add any safe-area CSS env() variables — that is deferred to Phase 25.
  </action>
  <verify>
    <automated>cd D:/Workspace/BATL && grep -q "viewport-fit=cover" frontend/index.html && echo "OK: viewport-fit=cover present"</automated>
  </verify>
  <done>Viewport meta tag includes viewport-fit=cover alongside existing width=device-width and initial-scale=1.0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify mobile dev tooling on real device</name>
  <action>Human verifies that the QR code, network-accessible dev server, and viewport meta tag all work correctly on a real mobile device.</action>
  <what-built>Mobile dev tooling: QR code terminal output, network-accessible Vite dev server, viewport-fit=cover meta tag</what-built>
  <how-to-verify>
    1. In one terminal, start the backend: `cd backend && npm run dev`
    2. In another terminal, run: `cd frontend && npm run dev:mobile`
    3. Verify a QR code appears in the terminal output along with a Network URL (http://192.168.x.x:3001)
    4. Scan the QR code with your phone (phone must be on the same Wi-Fi network)
    5. Confirm the app loads on your phone within 30 seconds
    6. Confirm API calls work (e.g., navigate to a page that loads data)
    7. In browser DevTools, inspect the viewport meta tag and confirm it shows viewport-fit=cover
    8. Stop dev:mobile and run `npm run dev` — confirm it works as before (localhost only, no QR code)
  </how-to-verify>
  <verify>Human confirms all 8 verification steps pass</verify>
  <done>QR code scan opens app on mobile device, API calls work through proxy, viewport-fit=cover present, regular dev script unchanged</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run dev:mobile` prints QR code and Network URL in terminal
- Scanning QR code opens the app on a mobile device
- API proxy works from mobile (data loads correctly)
- `npm run dev` remains unchanged (localhost only, no QR)
- viewport meta tag contains viewport-fit=cover
- vite-plugin-qrcode is listed in devDependencies (not dependencies)
- `npm run build` still succeeds (plugin is dev-only)
</verification>

<success_criteria>
Developer can scan a QR code from the terminal and test the app on a real mobile device within 30 seconds. The regular dev workflow is unchanged. The viewport meta tag supports device notch/home indicator for future mobile CSS work.
</success_criteria>

<output>
After completion, create `.planning/phases/20-mobile-dev-tooling/20-01-SUMMARY.md`
</output>
