---
phase: quick
plan: s51
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/TournamentsListPage.jsx
autonomous: true
must_haves:
  truths:
    - "Format column visible in desktop table between Category and End Date"
    - "Format badge visible on mobile cards"
    - "Format labels are translated (en/sk) using existing i18n keys"
  artifacts:
    - path: "frontend/src/pages/TournamentsListPage.jsx"
      provides: "Format column in tournament list table and mobile cards"
  key_links:
    - from: "TournamentsListPage.jsx"
      to: "tournament.formats.*"
      via: "i18n t() function"
      pattern: "t\\('tournament\\.formats\\."
---

<objective>
Add a Format column to the completed tournaments list page showing the tournament format type (Knockout, Group Stage, Combined, Swiss) as a new column in the desktop table and a badge on mobile cards.

Purpose: Users need to see at a glance what format each completed tournament used.
Output: Updated TournamentsListPage.jsx with format column/badge using existing backend data and i18n translations.
</objective>

<execution_context>
@C:/Users/erich/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/erich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/TournamentsListPage.jsx

The backend `listTournaments` API already returns `formatType` on each tournament object (enum: KNOCKOUT, GROUP, SWISS, COMBINED).
Existing i18n translations at `tournament.formats.knockout`, `tournament.formats.group`, `tournament.formats.swiss`, `tournament.formats.combined` in both en.json and sk.json.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add format column to TournamentsListPage</name>
  <files>frontend/src/pages/TournamentsListPage.jsx</files>
  <action>
1. Add `useTranslation` import from react-i18next at the top.
2. Inside the component, add `const { t } = useTranslation();`
3. Create a helper function to map formatType enum to i18n key:
   ```js
   const formatLabel = (formatType) => {
     const key = formatType?.toLowerCase();
     return key ? t(`tournament.formats.${key}`) : '-';
   };
   ```

4. DESKTOP TABLE: Add a "Format" column header `<th>` between "Category" and "End Date". Add corresponding `<td>{formatLabel(tournament.formatType)}</td>` in the table body row.

5. MOBILE CARDS: Add a second Badge (bg="info") showing the format label next to the existing category Badge. Use `formatLabel(tournament.formatType)`.

Do NOT add sorting or filtering UI — this is display-only for now.
  </action>
  <verify>
    <automated>cd D:/Workspace/BATL && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <done>
- Desktop table shows 4 columns: Tournament, Category, Format, End Date
- Mobile cards show category badge and format badge side by side
- Format values display translated labels (Knockout, Group Stage, etc.)
- Build succeeds with no errors
  </done>
</task>

</tasks>

<verification>
- `npx vite build --mode development` completes without errors
- Manual: visit /tournaments page, verify Format column appears in desktop view and badge in mobile view
</verification>

<success_criteria>
- Format type visible in both desktop table and mobile card layouts
- Translations work for all 4 format types (KNOCKOUT, GROUP, SWISS, COMBINED)
- No build errors
</success_criteria>

<output>
After completion, create `.planning/quick/260317-s51-add-format-type-to-the-tournament-list-a/260317-s51-SUMMARY.md`
</output>
