---
plan: 260329-4s4
one_liner: "CI pipeline status checking via gh CLI — post-push hook and pre-PR gate in CLAUDE.md"
requirements_completed: []
---

# Quick Task 260329-4s4: Summary

## What Was Done

1. **Created `scripts/check-ci.sh`** — reusable script that checks GitHub CI pipeline status for any branch using `gh run list` with `--jq` for single-call JSON parsing. Returns exit code 1 on failure, 0 on success/in-progress/no-runs.

2. **Added PostToolUse Bash hook** in `.claude/settings.local.json` — triggers after any Bash command containing `git push`, runs `check-ci.sh` automatically. Existing Write/Edit eslint hooks preserved.

3. **Added CLAUDE.md CI Pipeline Checks section** — instructs Claude to check CI before creating PRs and block on failure. Acts as the pre-ship gate since GSD workflows can't be modified at project level.

## Files Modified

- `scripts/check-ci.sh` (new) — CI status checker
- `.claude/settings.local.json` — PostToolUse Bash hook added
- `CLAUDE.md` — CI Pipeline Checks section added

## Verification

- `bash scripts/check-ci.sh --branch master` → "CI passed (CI) https://..."
- `bash scripts/check-ci.sh --branch 29-group-standings-and-tiebreakers` → "CI passed (CI) https://..."
- All 3 PostToolUse hooks present (Bash, Write, Edit)
